const routes = {
  home: '/home',
  login: '/login',
  register: '/register',
  contract: '/contract',
  request: '/request',
  support: '/support',
  terms: '/terms',
  profile: '/profile',

} as const

export default routes
